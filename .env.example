# Environment Configuration for Threat Modeling Platform

# ============================================
# Database Configuration
# ============================================
DB_ENCRYPTION_KEY=your-secure-encryption-key-here-change-in-production
DB_DATA_PATH=./data/db
DB_BACKUP_PATH=./data/backups

# ============================================
# API Backend Configuration (Node.js)
# ============================================
NODE_ENV=production
PORT=3001
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRY=24h
CORS_ORIGIN=http://localhost
LOG_LEVEL=info

# ============================================
# Analysis Service Configuration (Python)
# ============================================
FLASK_ENV=production
FLASK_APP=app.py

# LLM Provider Configuration
LLM_PROVIDER=openai
LLM_MODEL=gpt-4
MAX_TOKENS=4096
TEMPERATURE=0.7

# API Keys (Required for LLM analysis)
OPENAI_API_KEY=sk-your-openai-api-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# ============================================
# Frontend Configuration (React)
# ============================================
REACT_APP_API_URL=http://localhost/api
REACT_APP_ANALYSIS_URL=http://localhost/analysis

# ============================================
# Docker & Infrastructure
# ============================================
# Uncomment for production deployment
# COMPOSE_PROJECT_NAME=threat-model-platform
# COMPOSE_FILE=docker-compose.yml:docker-compose.prod.yml

# ============================================
# Security Notes
# ============================================
# 1. Generate secure keys using: openssl rand -hex 32
# 2. Never commit .env file with actual secrets
# 3. Use secrets management in production (e.g., AWS Secrets Manager, HashiCorp Vault)
# 4. Rotate keys regularly
# 5. Use separate keys for each environment (dev/staging/prod)
